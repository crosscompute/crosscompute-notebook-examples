# Add shell commands here to configure your environment automatically
# Note that this script will be running in parallel with the notebook server
# Run "cat /tmp/setup.log" in the notebook to check setup progress

# pip install scikit-learn
